server:
  port: 8080
  mode: production
  enable_swagger: false  # 生产环境禁用 Swagger

database:
  driver: sqlite
  dsn: "./data.db"

jwt:
  secret: "CHANGE-THIS-TO-STRONG-SECRET-IN-PRODUCTION"
  expire: 168h    # 7 days
  refresh_expire: 720h  # 30 days
