---
description: Go 后端开发规范
globs: server/**/*.go
alwaysApply: true
---

# Go 后端规范

## 分层结构

model → dto → validator → service → handler → router → swag init

框架层（config / dto / validator / model / store / middleware / service.admin /
handler.admin / router.SetupAdmin）在 github.com/xsxs89757/base-kit，模板里只有装配和业务代码。

下游业务代码的落点:
- `internal/model/<biz>/` - GORM 数据模型
- `internal/dto/<biz>/` - 请求/响应 DTO
- `internal/validator/<biz>/` - 请求验证规则
- `internal/service/<biz>/` - 业务逻辑
- `internal/handler/<biz>/` - HTTP 处理器 + Swagger 注解
- `internal/router/project.go` - 路由注册 + middleware.RegisterRoutePermissions 登记权限码
- `internal/store/project.go` - 模型登记与种子数据

## Swagger 注解

每个 handler 必须包含: @Summary, @Tags, @Accept, @Produce, @Param, @Success, @Router

Tags 命名规范:
- 后台: `认证`, `用户`, `系统管理 - 角色` 等
- 前台 (将来): `前台 - xxx`

生成命令: `swag init -g main.go -o docs --parseDependencyLevel 3 --packagePrefix base,github.com/xsxs89757/base-kit`
(parseDependencyLevel 3 让 swag 解析 module cache 里 base-kit 的 handler 注解)

## 响应格式

统一使用 `dto.Response`: `dto.Success(c, data)` / `dto.Fail(c, code, msg)` / `dto.PageSuccess(c, items, total)`

## 命名

- 文件: 小写下划线 `operation_log.go`
- 结构体/方法: 大驼峰 `GetUserList()`

## GORM 注意事项

- 含连续大写缩写的字段必须加 `gorm:"column:xxx"` 显式指定列名
  - `OID` → 自动变 `o_id`（期望 `oid`）→ 加 `gorm:"column:oid"`
  - `AIPDFPath` → 自动变 `a_ip_df_path`（期望 `ai_pdf_path`）→ 加 `gorm:"column:ai_pdf_path"`
  - 规律：GORM 在连续大写字母间插入下划线，多个缩写连写必出错
  - 正常驼峰不受影响：`FileName` → `file_name`，`UUID` → `uuid`
- 用 `map[string]any` 做 Updates 时，key 必须与数据库列名一致（snake_case），不是 Go 字段名
- 改了 model 字段后需要完整重启服务触发 AutoMigrate，air 热重载会自动重启但注意时序