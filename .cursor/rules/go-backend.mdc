---
description: Go 后端开发规范
globs: server/**/*.go
alwaysApply: false
---

# Go 后端规范

## 分层结构

model → dto → validator → service → handler → router → swag init

- `internal/model/admin/` - GORM 数据模型
- `internal/dto/admin/` - 请求/响应 DTO
- `internal/validator/admin/` - 请求验证规则
- `internal/service/admin/` - 业务逻辑
- `internal/handler/admin/` - HTTP 处理器 + Swagger 注解
- `internal/router/` - 路由注册
- `internal/middleware/` - JWT, Casbin, 操作日志

## Swagger 注解

每个 handler 必须包含: @Summary, @Tags, @Accept, @Produce, @Param, @Success, @Router

Tags 命名规范:
- 后台: `认证`, `用户`, `系统管理 - 角色` 等
- 前台 (将来): `前台 - xxx`

生成命令: `swag init -g main.go -o docs --parseDependency`

## 响应格式

统一使用 `dto.Response`: `dto.Success(c, data)` / `dto.Fail(c, code, msg)` / `dto.PageSuccess(c, items, total)`

## 命名

- 文件: 小写下划线 `operation_log.go`
- 结构体/方法: 大驼峰 `GetUserList()`

## GORM 注意事项

- 短缩写字段（如 `OID`）必须加 `gorm:"column:oid"` 显式指定列名，否则 GORM 会拆成 `o_id`
  - 规律：GORM 把 `ID` 当独立单词拆分，单字母+ID 会出错（`OID` → `o_id`）
  - 正常驼峰不受影响：`FileName` → `file_name`，`UUID` → `uuid`
- 用 `map[string]any` 做 Updates 时，key 必须与数据库列名一致（snake_case），不是 Go 字段名
- 改了 model 字段后需要完整重启服务触发 AutoMigrate，air 热重载会自动重启但注意时序
