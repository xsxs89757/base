# OS
.DS_Store
Thumbs.db

# IDE
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.history
.cursor
!.cursor/rules/
.claude

# Go
server/data.db
server/data.db-*
server/server
server/*.exe
server/tmp
server/config.yaml
server/config.prod.yaml
# server/docs 不再忽略：main.go import base/docs，fresh clone 若无该目录直接编译失败
# （"package base/docs is not in std"），Windows/CI 上未装 swag 就起不来。生成物随仓库提交，
# API 变更后用 swag init 重新生成并一起提交。

# Deploy
.deploy.env
.deploy.*.env
.build

# Node / Frontend
node_modules
.pnpm-store
dist
dist-ssr
dist.zip
dist.tar
dist.war
.nitro
.output
*-dist.zip
*-dist.tar
*-dist.war
coverage
*.local
**/.vitepress/cache
.cache
.turbo
.temp
dev-dist
.stylelintcache
yarn.lock
package-lock.json
.VSCodeCounter
**/backend-mock/data

# env local overrides
.env.local
.env.*.local
.eslintcache

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
vite.config.mts.*
vite.config.mjs.*
vite.config.js.*
vite.config.ts.*
.deploy.env
